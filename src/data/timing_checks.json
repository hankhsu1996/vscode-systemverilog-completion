{
    "$setup": {
        "detail": "$setup(data_event, reference_event, timing_check_limit, notifier)"
    },
    "$hold": {
        "detail": "$hold(reference_event, data_event, timing_check_limit, notifier)"
    },
    "$setuphold": {
        "detail": "$setuphold(reference_event, data_event, timing_check_limit, timing_check_limit, notifier, timestamp_condition, timecheck_condition, delayed_reference, delayed_data)"
    },
    "$recovery": {
        "detail": "$recovery(reference_event, data_event, timing_check_limit, notifier)"
    },
    "$removal": {
        "detail": "$removal(reference_event, data_event, timing_check_limit, notifier)"
    },
    "$recrem": {
        "detail": "$recrem(reference_event, data_event, timing_check_limit, timing_check_limit, notifier, timestamp_condition, timecheck_condition, delayed_reference, delayed_data)"
    },
    "$skew": {
        "detail": "$skew(reference_event, data_event, timing_check_limit, notifier)"
    },
    "$timeskew": {
        "detail": "$timeskew(reference_event, data_event, timing_check_limit, notifier, timestamp_condition, event_based_flag, remain_active_flag)"
    },
    "$fullskew": {
        "detail": "$fullskew(reference_event, data_event, timing_check_limit, timing_check_limit, notifier, event_based_flag, remain_active_flag)"
    },
    "$period": {
        "detail": "$period(controlled_reference_event, timing_check_limit, notifier)"
    },
    "$width": {
        "detail": "$width(controlled_reference_event, timing_check_limit, threshold, notifier)"
    },
    "$nochange": {
        "detail": "$nochange(reference_event, data_event, start_edge_offset, end_edge_offset, notifier)"
    }
}
